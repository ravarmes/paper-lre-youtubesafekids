# Copie para .env e preencha. O .env nao vai para o controle de versao.

# Chave da YouTube Data API v3, obtida no Google Cloud Console com a API habilitada.
# Sem ela a busca nao retorna nada: a plataforma consulta a API oficial, nao raspa
# a interface.
YOUTUBE_API_KEY=
